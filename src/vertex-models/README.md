# vertex-models

Lists Claude model IDs allowed on your Google Vertex AI project.

## Usage

```bash
vertex-models        # Claude models only
vertex-models --all  # All allowed models
```

## Requirements

- `gcloud` CLI authenticated
- `ANTHROPIC_VERTEX_PROJECT_ID` environment variable set
